name: Run curl every minute

on:
  schedule:
    - cron: '*/5 * * * *'  # Every minute
  workflow_dispatch:       # (Optional) allows manual trigger

jobs:
  curl_job:
    runs-on: ubuntu-latest
    steps:
      - name: Call API with curl
        run: curl https://ifconfig.net
